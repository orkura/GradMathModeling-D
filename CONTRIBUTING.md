# 团队协作规范

本仓库用于数学建模竞赛。协作时应优先保证原始数据安全、计算过程可复现、论文与代码结论一致。

## 分支与合并

- `main` 始终保留可运行、可编译的稳定版本，不直接强制推送。
- 每项工作使用独立分支，建议命名：
  - `model/<主题>`：模型设计、假设与推导；
  - `code/<主题>`：Python 实现与实验；
  - `paper/<主题>`：LaTeX 论文撰写与排版；
  - `data/<主题>`：数据清洗与转换；
  - `fix/<主题>`：错误修复。
- 开始工作前同步主分支：

  ```bash
  git switch main
  git pull --ff-only
  git switch -c code/example
  ```

- 合并前先同步 `main` 并解决冲突。避免多人同时编辑同一个大型二进制文件。

## 提交规范

一次提交只处理一个明确问题，提交信息建议使用以下前缀：

```text
模型: 完成路径规划约束定义
代码: 增加敏感性分析脚本
论文: 更新模型假设与符号表
数据: 添加清洗后的需求点坐标
修复: 修正目标函数单位换算
协作: 更新运行与复现说明
```

提交前检查：

```bash
git status
git diff --check
```

不要提交密码、令牌、个人环境配置、虚拟环境或临时编译文件。

## 数据管理

- `D题/数据/` 中的原始数据原则上只读；清洗、转换和特征构造通过脚本完成。
- 处理后的数据应注明来源、字段、单位和生成方法。
- CSV 等文本数据可以正常审查；MAT、TIFF、Office 文档等二进制文件修改前应在群内说明，避免覆盖冲突。
- 新增单个大文件前先检查体积。GitHub 拒绝超过 100 MB 的普通 Git 对象；较大成果应先讨论是否使用 Git LFS 或其他共享方式。

## Python 约定

- 使用项目本地虚拟环境 `.venv/`，不要将虚拟环境提交到仓库。
- 依赖确定后记录在 `requirements.txt` 或 `pyproject.toml` 中。
- 脚本使用相对路径，避免写入个人电脑的绝对路径。
- 随机算法固定随机种子，并记录关键参数、软件版本和运行入口。
- 图表和结果应能由代码重新生成；正式提交的关键结果可以保留，但应注明对应脚本。

## LaTeX 约定

- 主文档入口为 `Latex/MathModel.tex`，使用 XeLaTeX 编译。
- 推荐将中间文件输出到 `Latex/build/`：

  ```bash
  cd Latex
  latexmk -xelatex -outdir=build MathModel.tex
  ```

- `.tex`、`.bib`、图片和模板文件需要提交；辅助文件、日志和同步文件不提交。
- PDF 不做全局忽略，以便保留模板示例、参考资料和最终提交稿。更新最终论文 PDF 时，应同时提交对应的 LaTeX 源文件。
- 多人撰写时按章节拆分文件，并明确各章节负责人，减少对主文件的并行修改。

## 合并前自检

- Python 代码能够从干净环境运行，依赖和入口清楚。
- LaTeX 可以完整编译，没有缺图、缺字体或未解析引用。
- 模型符号、单位、图表数值及论文结论保持一致。
- 新增结果能够追溯到数据、参数和生成脚本。
- `git status` 中不包含缓存、临时文件或个人配置。

