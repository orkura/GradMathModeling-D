# 团队协作规范

本仓库用于数学建模竞赛。协作时应优先保证原始数据安全、计算过程可复现、论文与代码结论一致。

## 分支与合并

- `main` 始终保留可运行、可编译的稳定版本。禁止直接在 `main` 上提交或推送修改，所有变更必须在独立分支完成并通过合并进入 `main`。
- 原则上每项工作使用一个独立分支，避免在同一分支中混合无关修改。
- 分支统一采用 `<成员缩写>/<工作类型>/<主题>` 的格式。例如，罗财能负责 LaTeX 工程模板时，可使用 `lcn/paper/latex-template`。
- 工作类型建议使用以下名称：
  - `model`：模型设计、假设与推导；
  - `code`：Python 实现、实验与可视化；
  - `paper`：LaTeX 论文撰写与排版；
  - `data`：数据清洗、转换与特征构造；
  - `fix`：错误修复。
- `<主题>` 应简短、明确，使用小写英文字母、数字和连字符，避免使用 `test`、`new` 等含义模糊的名称。
- 开始工作前，先同步远程 `main`，再创建工作分支：

  ```bash
  git switch main
  git pull --ff-only
  git switch -c lcn/code/example
  ```

- 提交合并前，应将最新的 `main` 同步到工作分支，解决冲突并完成“合并前自检”。仅由一人使用的分支可执行：

  ```bash
  git fetch origin
  git rebase origin/main
  ```

- 合并请求应说明修改目的、主要内容、验证方法及已知限制；涉及模型结果、图表或论文结论时，应附上对应的数据、参数和生成方式。
- 合并完成后及时删除已无用途的工作分支。避免多人同时编辑同一个大型二进制文件；确需修改时，应提前协调负责人和修改时段。

## 提交规范

一次提交只处理一个明确问题。提交信息必须以提交者的成员缩写开头，统一使用 `<成员缩写>/<类型>: <说明>` 的格式，例如：

```text
lcn/模型: 完成路径规划约束定义
lcn/代码: 增加敏感性分析脚本
lcn/论文: 更新模型假设与符号表
lcn/数据: 添加清洗后的需求点坐标
lcn/修复: 修正目标函数单位换算
lcn/协作: 更新运行与复现说明
```

提交前检查：

```bash
git status
git diff --check
```

不要提交密码、令牌、个人环境配置、虚拟环境或临时编译文件。

## 数据管理

- `D题/数据/` 中的原始数据原则上只读；清洗、转换和特征构造通过脚本完成。
- 处理后的数据应注明来源、字段、单位和生成方法。
- CSV 等文本数据可以正常审查；MAT、TIFF、Office 文档等二进制文件修改前应在群内说明，避免覆盖冲突。
- 新增单个大文件前先检查体积。GitHub 拒绝超过 100 MB 的普通 Git 对象；较大成果应先讨论是否使用 Git LFS 或其他共享方式。

## Python 约定

- python 的虚拟环境根据自己的情况而定。
- 依赖确定后记录在 `requirements.txt` 或 `pyproject.toml` 中。
- 脚本使用相对路径，避免写入个人电脑的绝对路径。
- 随机算法固定随机种子，并记录关键参数、软件版本和运行入口。
- 图表和结果应能由代码重新生成；正式提交的关键结果可以保留，但应注明对应脚本。

## LaTeX 约定

- 主文档入口为 `Latex/MathModel.tex`，使用 XeLaTeX 编译。
- 推荐将中间文件输出到 `Latex/build/`：

  ```bash
  cd Latex
  latexmk -xelatex -outdir=build MathModel.tex
  ```

- `.tex`、`.bib`、图片和模板文件需要提交；辅助文件、日志和同步文件不提交。
- PDF 不做全局忽略，以便保留模板示例、参考资料和最终提交稿。更新最终论文 PDF 时，应同时提交对应的 LaTeX 源文件。
- 多人撰写时按章节拆分文件，并明确各章节负责人，减少对主文件的并行修改。

## 合并前自检

- Python 代码能够从干净环境运行，依赖和入口清楚。
- LaTeX 可以完整编译，没有缺图、缺字体或未解析引用。
- 模型符号、单位、图表数值及论文结论保持一致。
- 新增结果能够追溯到数据、参数和生成脚本。
- `git status` 中不包含缓存、临时文件或个人配置。
